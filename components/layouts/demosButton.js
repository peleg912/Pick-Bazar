import React from 'react';

const demosButton = (props)=> {
    return(
        <button className="demos-btn">
            <span className="demos-title">Demos</span>
        </button>
    )
}

export default demosButton;